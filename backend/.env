# ============================================
# 🌐 ENVIRONMENT
# ============================================
# Set to 'production' when deploying to Azure
# Set to 'development' for local development
NODE_ENV=development

# ============================================
# 🚀 SERVER CONFIGURATION
# ============================================
# Azure Static Web Apps will provide PORT automatically
PORT=5000

# ============================================
# 💻 LOCAL DEVELOPMENT (PostgreSQL on your laptop)
# ============================================
# Local PostgreSQL database connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pipeline_dashboard
DB_USER=dashboard_admin
DB_PASSWORD=powertransition
DB_SCHEMA=pipeline_dashboard

# ============================================
# ☁️ AZURE PRODUCTION (Azure PostgreSQL - UNCOMMENT WHEN DEPLOYING)
# ============================================
# Uncomment and update these when deploying to Azure
# DB_HOST=pt-power-pipeline-db.postgres.database.azure.com
# DB_PORT=5432
# DB_NAME=pt_power_pipeline
# DB_USER=pt_admin
# DB_PASSWORD=PowerTransition@2024!
# DB_SCHEMA=public  # Azure uses 'public' schema by default

# ============================================
# 🔗 CORS CONFIGURATION
# ============================================
# Local development frontend URL (Vite default)
FRONTEND_URL=http://localhost:5173

# For Azure Static Web Apps, frontend will be served from same domain
# No CORS issues since API routes are under /api/*

# ============================================
# 🔐 SECURITY (CHANGE THESE FOR PRODUCTION!)
# ============================================
# Generate a secure JWT secret for production:
# In terminal: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Session/cookie settings
JWT_EXPIRES_IN=24h
COOKIE_SECURE=false  # Set to 'true' in production with HTTPS
COOKIE_SAME_SITE=lax

# ============================================
# 📧 EMAIL CONFIGURATION (if using email service)
# ============================================
# SMTP_SERVER=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=your-email@company.com
# SMTP_PASSWORD=your-email-password
# EMAIL_FROM=noreply@yourdomain.com

# ============================================
# ⚠️ IMPORTANT NOTES
# ============================================
# 1. NEVER commit this file to GitHub with production values!
# 2. Add `.env` to your `.gitignore` file
# 3. For Azure production, set environment variables in:
#    - Azure Portal → Static Web App → Configuration
#    - Or use Azure Key Vault for sensitive values
# 4. Create a separate `.env.production` file for production values
#    but do not commit it to version control